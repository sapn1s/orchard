VERDICT: BROKEN
CLAIM: every Wiring layer must be reported from its true source on each call (registry refs plus files on disk), a status read must never scaffold or shell out, onboard-Apply must run only against the registry hostPath, idempotently, never the station's own tree, attach-WA must go through the validated project PATCH, and hostPath must never come from the request body.
FIXER-TEST: run 0cd18502760c
ADVERSARIAL: wa-derivation-and-dead-end-applies run 88db700b9555
WHY-UNCOVERED: the fixture only ever builds two CLAUDE.md shapes — onboard's own authored pointer and one with no mention of a WA at all — and never a pre-existing repo whose CLAUDE.md merely names the token or points at a path that does not exist, nor a project whose docs/CONVENTIONS.md exists but is blank, nor one with no package.json, so the substring-only WA derivation and the Applies that cannot flip their own row are never exercised.
ADVERSARIAL: apply-idempotency-hostpath-and-read-inertness run 06b4c0e1ddbf
WHY-UNCOVERED: the fixture applies onboard exactly once to a repo with no pre-existing methodology artifacts, never sends hostPath/dir/targetDir in the Apply body, never attaches the WA twice over a stack that already holds an unrelated ref plus a disabled WA ref, and never reads a project whose registered hostPath was deleted — this run exercises all of those, and its single FAIL is my own over-strict assertion, since onboard legitimately adds the absent board:gen/arch:watch keys while leaving the repo's custom board:check intact.
UNTESTED: the client-side Wiring panel's armed "Scaffold into <project>" confirm, its busy/disable interlock and the wiringReport created-count regex — the browser leg needs brave over CDP and I drove only the server API, so a mislabelled or double-firing Apply button in the drawer would not surface in my runs.
UNTESTED: the read-modify-write window in the attach-wa route, which snapshots the project before awaiting the request body and then PATCHes the whole instructions array — I could not deterministically interleave a competing PATCH inside that window over plain fetch, so whether a concurrent instruction-stack edit is silently lost is unresolved.
UNTESTED: whether a WA ref that is enabled but whose template no longer exists in the library is actually injected at launch — I confirmed the ref persists and the row reads ok, but did not start a real session to check the Working Agreement text reaches the prompt.
UNTESTED: the cleanroom copy shipped without docs/prompts and docs/bugs, so the station server crashed at seedTemplates on my first attempt and I created placeholder seed files to make the suite runnable, meaning the WA and pattern template bodies under test are stubs rather than the real ones.
FINDING: the Working Agreement layer is derived from a bare case-insensitive WORKING_AGREEMENT substring match over the project's CLAUDE.md in claudeMdHasWaPointer (src/server/wiring.ts), so a repo whose CLAUDE.md states it deliberately does NOT use the shared WORKING_AGREEMENT is reported state ok with detail "points at the shared Working Agreement" and apply null — the panel asserts a layer is applied when it is not and offers no remedy.
FINDING: that same check never verifies the pointer resolves, contradicting its own "resolvable Working-Agreement pointer" contract — a CLAUDE.md referencing docs/prompts/WORKING_AGREEMENT.md when no such file exists in the project still reports Working Agreement ok (src/server/wiring.ts).
FINDING: a project with an existing but blank docs/CONVENTIONS.md reports Local conventions missing with apply onboard, yet the Apply returns 200 and changes nothing because onboard's ensureFile skips the existing empty file, leaving the row permanently missing with a button that can never flip it (src/server/index.ts wiring apply into scripts/onboard.mjs ensureFile).
FINDING: a project with no package.json still reports Board drift-guard warn with apply onboard after the Apply has already run, because onboard cannot install a board:check script without a package.json, so the advertised remedy is a permanent no-op for that row (src/server/wiring.ts drift-guard check, scripts/onboard.mjs installBoardTool).