VERDICT: BROKEN
CLAIM: Every methodology layer the Wiring panel reports is derived from its true source on every call, reads never scaffold or shell out, onboard-Apply runs only against the registry hostPath and never clobbers existing artifacts, and attach-WA goes through the validated project PATCH path.
FIXER-TEST: run 938641989cc4
ADVERSARIAL: provider-routing-layer-is-hardcoded-not-derived run f4ff100a1e09
WHY-UNCOVERED: The author's fixture never varies the routing layer's true source (`<repo>/docs/prompts/ROUTING.md`, read by `routingSection()` and folded into sessions by `composeInstructions(refs,{hostPath,routing:true})`); its only assertion on that row is `state === 'info'`, true by construction, so it cannot detect that the row is a constant.
ADVERSARIAL: wa-ref-points-at-a-deleted-template run e7694f251dd3
WHY-UNCOVERED: The author's fixture attaches a WA ref against a freshly-seeded template library and never removes a template, so it never produces a project whose enabled WA ref no longer resolves — the case where the ref list alone is not the true source of "the Working Agreement is injected".
UNTESTED: The client-side stale-status race in public/lib/drawer.js — `wiringGroup()` fires `refreshWiring(p.id)` during paint into the single shared `d.wiring` slot, so an in-flight reply for project A can land after `repaint()` switched to project B; I could not force the two responses out of order deterministically over CDP without a server-side delay hook, so it is neither confirmed nor cleared.
UNTESTED: Behaviour against the real unstripped tree — `docs/prompts/*` and `docs/bugs/*` were absent in this cleanroom copy and the server refused to boot (seedTemplates ENOENT) until I wrote stub seed files, so the real routing mirror content and the station's real board were never in play.
UNTESTED: Concurrency and permission failure modes of onboard-Apply — two simultaneous POSTs to `/wiring/apply` on one project, and a read-only or partially-writable target tree; I exercised only the serial, writable path.
UNTESTED: The production `:4317` service path the ticket says needs a restart to serve the new route; every run used a scratch server on an ephemeral port with a scratch dataDir.
FINDING: src/server/wiring.ts — the "Provider routing" row is a hardcoded `state:'info'` plus "Universal — folded into every session launched from Orchard", derived from nothing: with `docs/prompts/ROUTING.md` absent or empty `routingSection()` returns null and a launched session gets no routing section, yet the panel still reports the layer as applied, and the row is byte-identical whether the mirror is present or absent.
FINDING: src/server/wiring.ts `hasEnabledWaRef()` — it reads only `project.settings.instructions` and never checks that the templateId still resolves; after `DELETE /api/templates/working-agreement-v2` the server's own `/api/compose/:id` reports `mode:'none', missingIds:['working-agreement-v2'], chars:0` while the WA row still shows `state:'ok'`, "injected into every session launched here", with `apply:null` — a ✅ that cannot be repaired from the panel.